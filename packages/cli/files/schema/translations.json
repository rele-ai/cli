{
    "type": "object",
    "required": [
        "type",
        "key"
    ],
    "properties": {
        "type": {
            "type": "string",
            "enum": [
                "Translation"
            ]
        },
        "key": {
            "type": "string",
            "pattern": "^[a-z0-9_]+$"
        },
        "lang": {
            "type": "string",
            "pattern": "^([a-z]{2})(_[a-z]{2})?$"
        },
        "value": {
            "type": "string"
        }
    }
}